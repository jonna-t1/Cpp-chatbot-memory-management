{
    "version": "2.0.0",
    "tasks": [
            {
                "label": "buildDir",
                "type": "shell",
                "command": "mkdir -p build; cd build && cmake -DCMAKE_BUILD_TYPE=Debug .. && make"
            }
    ]
}